import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6112B0",
    },
    textPrimary: {
      main: "#ffff",
    },
    black: {
      main: "#0000",
    },
    secondary: {
      main: "#430c81",
    },
  },
});
export default theme;
